# Base URL of the deployed FastAPI backend (Railway will inject once linked).
# For local dev against a locally-run backend on port 8000, use http://localhost:8000
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
